// Welcome to EXPLORER's IDE

#include<stdio.h>
#include<math.h>
#include<string.h>

int main() {
    /*
     strdup() is used to duplicate anything from a string to a string
     syntax: strdup(str);
     */
    char *str="Surya";
    char *p=strdup(str);
p=strdup(str);
printf("\nDuplicate String:%s\n",p);
}
