// Welcome to EXPLORER's IDE

#include<stdio.h>
#include<math.h>
#include<string.h>

int main() {
/*
 strcat() is used to concatenate str2 at the end of str1
Syntax: strcat(str1,str2);
 */
char str1[100]="Surya ";
char str2[]="Ghosh";
   strcat(str1,str2);
    puts(str1);
    return 0;
}
