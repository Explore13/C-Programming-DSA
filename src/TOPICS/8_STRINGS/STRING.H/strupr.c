// Welcome to EXPLORER's IDE

#include<stdio.h>
#include<math.h>
#include<string.h>

int main() {
/*
 strupr() is used to make the lower case into
 uppercase
 syntax: strupr(str);
 */
char str[]="surya ghosh";
printf("%s",strupr(str));
    return 0;
}
