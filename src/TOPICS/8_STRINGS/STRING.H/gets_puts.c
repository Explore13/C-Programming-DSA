//WELCOME TO EXPLORER's IDE :)

#include<stdio.h>
#include<string.h>
#include<math.h>

int main() {
char str[100];
/*
 we can use gets() function in the place of scanf() function...
 and also can use puts() function in the place of printf() function...
 */
gets(str);
puts(str);

    return 0;
}