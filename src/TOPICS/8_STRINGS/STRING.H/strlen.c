// Welcome to EXPLORER's IDE

#include<stdio.h>
#include<math.h>
#include<string.h>

int main() {
/*
 Used to determine the length of a string..
 Syntax: strlen(str);
 */
char str[]="SURYA THE EXPLORER";
printf("%d",strlen(str));
return 0;
}
